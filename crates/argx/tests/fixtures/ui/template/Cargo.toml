[package]
name = "argx-ui"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
argx = { path = "../../../.." }

# Keep this fixture independent from the repository workspace. Its lockfile models the
# standalone downstream projects created by the compiler-UI test harness.
[workspace]
